[build-system]
requires = ["flit"]
build-backend = "flit.buildapi"

[project]
name = "pollen"
authors = [{name = "Susan Garry", email = "shg64@cs.cornell.edu"}]
readme = "README.md"
dynamic = ["version", "description"]
dependencies = []

[project.scripts]
exine = "pollen.main:main"
