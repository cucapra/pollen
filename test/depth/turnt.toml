[envs.baseline]
binary = true
command = "odgi build -g {filename} -o temp.og; odgi depth -i temp.og -d; rm temp.og"

[envs.calyx]
binary = true
command = "odgi build -g {filename} -o temp.og; pollen depth --action=run -f temp.og -a temp.og; rm temp.og"