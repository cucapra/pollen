[package]
name = "flatgfa-c"
version = "0.1.0"
edition = "2021"

[lib]
name = "flatgfa"
crate-type = ["cdylib", "staticlib"]  # produces .so/.a for C linking

[dependencies]
flatgfa = { path = "../flatgfa" }
bstr = { workspace = true }

[build-dependencies]
cbindgen = "0.27"
